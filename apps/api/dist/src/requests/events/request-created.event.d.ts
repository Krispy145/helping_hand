export declare class RequestCreatedEvent {
    readonly requestId: string;
    readonly title: string;
    readonly description: string;
    constructor(requestId: string, title: string, description: string);
}
